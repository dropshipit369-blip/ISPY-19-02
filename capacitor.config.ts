import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.dropshipit369.ispyprofittool",
  appName: "iSpy",
  webDir: "dist",
  server: {
    androidScheme: "https",
  },
};

export default config;
